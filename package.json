{
  "name": "weibo-agent-dashboard",
  "version": "0.4.0",
  "private": true,
  "description": "Free-first personal Weibo content dashboard with a whitelisted Weibo CLI bridge.",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build -w packages/weibo-bridge && concurrently -n server,web -c green,cyan \"npm run dev -w apps/server\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w packages/weibo-bridge && npm run build -w apps/server && npm run build -w apps/web",
    "build:web:mock": "npm run build -w apps/web",
    "start": "node apps/server/dist/index.js",
    "lint": "npm run lint --workspaces --if-present",
    "test:smoke": "npm run build && node scripts/smoke-test.mjs",
    "probe:weibo-cli": "node scripts/probe-weibo-cli.mjs",
    "test:real-cli:smoke": "node scripts/real-cli-smoke.mjs",
    "check:public-demo": "node scripts/check-public-demo.mjs",
    "check:server-health": "node scripts/check-server-health.mjs",
    "screenshots": "node scripts/capture-screenshots.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "playwright": "^1.61.1",
    "typescript": "^5.8.3"
  }
}
